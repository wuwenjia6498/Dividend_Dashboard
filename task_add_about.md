# 任务：新增“使用说明”功能 (About Feature)

## 目标
在首页标题栏增加一个“帮助/说明”按钮，点击后弹出对话框，向用户介绍本项目的核心逻辑、信号含义以及风控规则。

## 1. 新建组件 `components/AboutDialog.tsx`
请基于 Shadcn UI 的 Dialog 组件实现。

### UI 要求：
- **触发按钮：** 使用 Ghost 变体的 IconButton，图标使用 `lucide-react` 的 `HelpCircle`，颜色为灰色 (`text-gray-500`)，hover 时变深。
- **弹窗内容：** `max-w-2xl`，高度最大 `85vh`，允许纵向滚动。
- **排版风格：** 请使用语义化的 HTML (h3, ul, li, p) 配合 Tailwind 类名来实现类似 Markdown 的文档排版。使用 `lucide-react` 的图标来增强标题的可读性。

### 核心文案 (必须严格包含以下所有内容)：

#### 1. 核心设计理念
本项目是一个结合**“基本面排雷”**与**“量化择时”**的投资辅助工具。
我们不预测短期股价涨跌，而是通过长期数据的统计规律，寻找**“好价格”**与**“好资产”**的交集。
> **核心原则：** 即使是好公司，买贵了也是一笔坏投资。

#### 2. 🚦 红绿灯信号系统 (择时策略)
基于过去 **5 年 (约 1250 个交易日)** 的历史股息率数据，计算当前估值所处的位置（分位点）：
* 🟢 **机会区间 (分位点 > 80%)**：**“低价高息区”**。当前股息率高于历史上 80% 的时间。股价相对低估，具备较高的安全边际。
* 🔴 **风险区间 (分位点 < 20%)**：**“高价低息区”**。当前股息率低于历史上 80% 的时间。股价可能透支未来增长，建议谨慎。
* 🟡 **持有区间 (20% - 80%)**：**“合理估值区”**。股价处于正常波动范围。

#### 3. 📊 股息率通道图解读
详情页的“股息率通道”将枯燥的百分比转化为直观的**“价格跑道”**：
* **下轨 (机会线)：** 对应 **80% 分位点** 的理论股价。实际股价跌破此线 = 极具吸引力。
* **上轨 (风险线)：** 对应 **20% 分位点** 的理论股价。实际股价突破此线 = 缺乏吸引力。
* **使用逻辑：** 股价在通道**下方**为低估；在**中间**为合理；在**上方**为高估。

#### 4. 🛡️ 现金流排雷规则 (核心风控)
* **强制使用“经营现金流 (OCF)”**：
  我们**不使用**自由现金流 (FCF) 进行排雷。因为银行、运营商等高股息行业通常资本开支巨大导致 FCF 为负。使用 OCF 能更真实地反映其造血能力。
* **数据清洗**：
  剔除了异常尖刺，并针对**除权除息日**导致的 TTM 数据断层做了平滑处理。

#### 5. ⚙️ 数据更新机制
* **每日自动更新**：每晚 **20:00** 自动运行。
* **新增股票**：添加后立即触发后台单只抓取，等待 **1-2 分钟** 后自动显示并高亮，**无需手动刷新**。

## 2. 集成到页面
请将此组件放置在 `app/page.tsx` (或 Header 组件) 中。
**位置要求：** 放在现有的 `RefreshButton` (刷新按钮) 的**左侧**，形成功能按钮组。